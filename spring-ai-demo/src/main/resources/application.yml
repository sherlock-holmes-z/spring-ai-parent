server:
  port: 8081

spring:
  application:
    name: spring-ai-demo
  ai:
    zhipuai:
      api-key: ${ZHIPU_KEY}
      base-url: https://open.bigmodel.cn/api/paas/v4
      chat:
        options:
          model: glm-4.7